apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bewritten.fullname" . }}
  labels:
    {{- include "bewritten.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bewritten.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bewritten.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml .Values.topologySpreadConstraints | nindent 8 }}
      {{- else }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              {{- include "bewritten.selectorLabels" . | nindent 14 }}
      {{- end }}
      {{- if .Values.affinity }}
      affinity:
        {{- toYaml .Values.affinity | nindent 8 }}
      {{- else }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    {{- include "bewritten.selectorLabels" . | nindent 20 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: BEWRITTEN_API_PORT
              value: "8080"
            - name: BEWRITTEN_ADMIN_EMAIL
              value: {{ .Values.env.BEWRITTEN_ADMIN_EMAIL | quote }}
            - name: BEWRITTEN_ADMIN_PASSWORD
              value: {{ .Values.env.BEWRITTEN_ADMIN_PASSWORD | quote }}
            - name: BEWRITTEN_SECRET_KEY
              value: {{ .Values.env.BEWRITTEN_SECRET_KEY | quote }}
            - name: BEWRITTEN_AI_RATE_WINDOW_SECONDS
              value: {{ .Values.env.BEWRITTEN_AI_RATE_WINDOW_SECONDS | quote }}
            - name: BEWRITTEN_AI_RATE_MAX
              value: {{ .Values.env.BEWRITTEN_AI_RATE_MAX | quote }}
            {{- if .Values.redis.url }}
            - name: BEWRITTEN_REDIS_URL
              value: {{ .Values.redis.url | quote }}
            {{- else if .Values.redis.enabled }}
            {{- if .Values.redis.password }}
            - name: BEWRITTEN_REDIS_URL
              value: "redis://:{{ .Values.redis.password }}@{{ include "bewritten.fullname" . }}-redis:{{ .Values.redis.service.port }}/0"
            {{- else }}
            - name: BEWRITTEN_REDIS_URL
              value: "redis://{{ include "bewritten.fullname" . }}-redis:{{ .Values.redis.service.port }}/0"
            {{- end }}
            {{- end }}
            {{- if .Values.postgres.enabled }}
            - name: DB_HOST
              value: "{{ include "bewritten.fullname" . }}-postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: {{ .Values.postgres.auth.username | quote }}
            - name: DB_PASSWORD
              value: {{ .Values.postgres.auth.password | quote }}
            - name: DB_NAME
              value: {{ .Values.postgres.auth.database | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
